#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
* main print if the number is positive or negative or zero
* Desciption using the main function
* this program will print either negative zero or positive
* return zero
*/
int main(void)
{
int n;
srand(time(o))
n = rand() - RAND_MAX / 2;
if(n > 0)
{
printf("%d is positive\n", n);
}
else if (n == 0)
{
printf("%d is zero\n", n);
}
else if  (n < 0)
{
printf("%d is negative\n", n);
}
return (0);
}
